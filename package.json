{
  "name": "sui-wallet-generator-custom",
  "version": "1.0.0",
  "description": "A script to generate multiple Sui wallets (Address, Base64 PrivateKey, Mnemonic) with console output and file saving.",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "sui",
    "wallet",
    "generator",
    "crypto",
    "blockchain",
    "mnemonic",
    "private key",
    "mysten"
  ],
  "author": "Your Name / Shashi",
  "license": "MIT",
  "dependencies": {
    "@mysten/sui.js": "^0.54.1",  // <-- Sui TypeScript SDK (check for latest version)
    "chalk": "^5.3.0",
    "moment": "^2.30.1",
    "readline-sync": "^1.4.10",
    "buffer": "^6.0.3"            // <-- For Base64 encoding
  }
}
